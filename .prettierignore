# These aren't really YAML files, they are Go templates which render to YAML.
/helm/templates/*.yaml

# Prettier disagrees with Helm's internal lint tool.
/helm/values.yaml

# Keep it exactly as generated by Helm
/tests/helm/template/*.yaml
/app/functions/helmless/default-values.yaml

# Go template files that render to YAML - don't format template syntax
/tests/kind/testdata/kind-config.yaml
