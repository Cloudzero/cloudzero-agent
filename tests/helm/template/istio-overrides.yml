# Istio integration test configuration
# This tests the DestinationRule/VirtualService generation and port exclusion annotations

cloudAccountId: "1234567890"
clusterName: "istio-test-cluster"
region: "us-east-1"
apiKey: "not-a-real-api-key"

# For testing only, you should never use this property in production.
jobConfigID: "DEADBEEF-FEED-FACE-CAFE-FEE10D15EA5E"

# Explicitly enable Istio integration (normally auto-detected via CRDs)
integrations:
  istio:
    enabled: true
    clusterID: "istio-cluster-id-for-testing"
