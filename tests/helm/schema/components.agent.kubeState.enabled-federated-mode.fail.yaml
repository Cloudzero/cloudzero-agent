# Invalid: kubeState enabled with federated mode
# The KubeState plugin requires mode to be "clustered"
apiKey: "test-key-123"
existingSecretName: null
components:
  agent:
    mode: federated
    kubeState:
      enabled: true
