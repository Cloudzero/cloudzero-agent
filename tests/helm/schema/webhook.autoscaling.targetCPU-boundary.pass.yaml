# Valid: CPU target at boundary (1%)
apiKey: "test-key-123"
existingSecretName: null
components:
  agent:
    autoscaling:
      enabled: false
  webhookServer:
    autoscaling:
      enabled: true
      minReplicas: 2
      maxReplicas: 10
      targetCPUUtilizationPercentage: 1
insightsController:
  enabled: true
