# Invalid: defaults.env uses the strict Kubernetes EnvVar schema (value must be
# a string). The `value: null` tombstone is only supported on per-component env,
# where it has meaning (removing an inherited variable); in defaults.env it
# would be meaningless and is rejected.
apiKey: "test-key-123"
existingSecretName: null
defaults:
  env:
    - name: FOO
      value: null
