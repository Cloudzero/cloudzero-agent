# Valid: a component env entry may set a normal value and may use an explicit
# `value: null` tombstone to remove a variable inherited from defaults.env.
apiKey: "test-key-123"
existingSecretName: null
defaults:
  env:
    - name: SOMETHING
      value: from-defaults
components:
  aggregator:
    shipper:
      env:
        - name: GOMAXPROCS
          value: "4"
        - name: SOMETHING
          value: null
