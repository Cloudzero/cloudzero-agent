# Invalid: CPU target must be <= 100
apiKey: "test-key-123"
existingSecretName: null
insightsController:
  enabled: true
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 101
