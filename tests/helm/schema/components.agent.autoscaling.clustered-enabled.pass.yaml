apiKey: "test-key-123"
existingSecretName: null
defaults:
  federation:
    enabled: false
components:
  agent:
    mode: clustered
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 10
