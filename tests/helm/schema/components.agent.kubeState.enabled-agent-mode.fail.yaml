# Invalid: kubeState enabled with agent mode
# The KubeState plugin requires mode to be "clustered"
apiKey: "test-key-123"
existingSecretName: null
components:
  agent:
    mode: agent
    kubeState:
      enabled: true
